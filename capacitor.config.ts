import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Gerenciador de cronogramas',
  webDir: 'www'
};

export default config;
